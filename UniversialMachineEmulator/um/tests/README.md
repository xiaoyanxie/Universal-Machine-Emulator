# Tests

## Files
- `tests/UMTESTS.txt`: Expected UM test names.
- `tests/generated/`: Generated UM test programs and expected I/O fixtures.

## Generation
If `writetests` exists in your environment, run it from this directory and place outputs in `tests/generated/`.
